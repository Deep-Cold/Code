#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N=150;
const int Mod=998244353;
int t,m;
ll n;
int num[N];
int main()
{
    ios::sync_with_stdio(false);
    cin>>t;
    while(t--)
    {
        cin>>n>>m;
        int k=log2(n);
        for(int i=0;i<=k;i++)
            for(int j=0;j<i;j++)
                for(int u=0;u<)
    }
    return 0;
}